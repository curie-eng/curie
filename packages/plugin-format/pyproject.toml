[project]
name = "plugin-format"
version = "0.0.0"
description = "Claude Code plugin bundle format (verbatim) and its validators"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.9",
    "pyyaml>=6.0",
    "tzdata>=2025.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/plugin_format"]
