{{- if and .Values.inference.deploy .Values.inference.pullModel (not .Values.inference.persistence.enabled) }}
{{- fail "inference.deploy=true with inference.pullModel=true would implicitly download model weights into an ephemeral emptyDir; enable durable storage with inference.persistence.enabled=true, or pre-provision the model and set inference.pullModel=false." }}
{{- end }}
{{- if .Values.inference.deploy }}
{{- if .Values.inference.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "curie.fullname" . }}-inference
  labels:
    {{- include "curie.labels" . | nindent 4 }}
    app.kubernetes.io/component: inference
spec:
  accessModes: ["ReadWriteOnce"]
  {{- if .Values.global.storageClass }}
  storageClassName: {{ .Values.global.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.inference.persistence.size }}
---
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "curie.fullname" . }}-inference
  labels:
    {{- include "curie.labels" . | nindent 4 }}
    app.kubernetes.io/component: inference
spec:
  selector:
    {{- include "curie.selectorLabels" (dict "root" . "component" "inference") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.inference.service.port }}
      targetPort: {{ .Values.inference.port }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "curie.fullname" . }}-inference
  labels:
    {{- include "curie.labels" . | nindent 4 }}
    app.kubernetes.io/component: inference
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "curie.selectorLabels" (dict "root" . "component" "inference") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- with (include "curie.placement.labels" (dict "root" $ "class" "platform")) }}
        {{- . | nindent 8 }}
        {{- end }}
        {{- include "curie.labels" . | nindent 8 }}
        {{- include "curie.selectorLabels" (dict "root" . "component" "inference") | nindent 8 }}
      {{- with (include "curie.placement.annotations" (dict "root" $ "class" "platform")) }}
      annotations:
        {{- . | nindent 8 }}
      {{- end }}
    spec:
      {{- with (include "curie.placement.spec" (dict "root" $ "class" "platform")) }}
      {{- . | nindent 6 }}
      {{- end }}
      volumes:
        - name: ollama-data
          {{- if .Values.inference.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "curie.fullname" . }}-inference
          {{- else }}
          emptyDir: {}
          {{- end }}
      containers:
        - name: ollama
          image: {{ .Values.inference.image | quote }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          {{- with .Values.inference.containerSecurityContext }}
          {{- include "curie.containerSecurityContext" . | nindent 10 }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.inference.port }}
          {{- if .Values.inference.pullModel }}
          # Pull after the server process starts, since `ollama pull` needs the
          # local API to be accepting requests inside the same container.
          lifecycle:
            postStart:
              exec:
                command:
                  - /bin/sh
                  - -c
                  - |
                    set -eu
                    for i in $(seq 1 60); do
                      if ollama list >/dev/null 2>&1; then
                        ollama pull {{ .Values.inference.model | quote }}
                        exit 0
                      fi
                      sleep 2
                    done
                    exit 1
          {{- end }}
          readinessProbe:
            tcpSocket:
              port: {{ .Values.inference.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 12
          livenessProbe:
            tcpSocket:
              port: {{ .Values.inference.port }}
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            {{- toYaml .Values.inference.resources | nindent 12 }}
          volumeMounts:
            - name: ollama-data
              mountPath: /root/.ollama
{{- end }}
