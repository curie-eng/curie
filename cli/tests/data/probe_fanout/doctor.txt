MISS  Model credential           none found
      → export CURIE_CREDENTIALS=sk-ant-...   (or `curie secrets set CURIE_CREDENTIALS`; `curie skill up --fake-model` needs none)
--    Model pin                  no model determined: per-agent overrides could not be read because the platform API was not reached, no release default agentSandbox.runner.model or inference.model, and no CURIE_MODEL
ok    Docker                     running
MISS  Bundle in this directory   no .claude-plugin/plugin.json
      → curie init my-agent && cd my-agent
ok    Cluster                    doctor-stub-context (reached)
ok    Curie release              curie (curie-0.8.2)
--    Mail channel token         mail adapter not deployed
ok    Completion delivery        completion outbox: state empty; count 0; inflight 0; retry 0; terminal 0; oldest age 0s
--    Slack                      no tokens recorded; reachable with `curie cluster message`
MISS  Clone credential           none — a private repo cannot be cloned, so git-push deploys will fail
      → curie cluster github-app --namespace curie --release curie --app-id <id> --private-key ./key.pem
ok    Push delivery              NodePort 30800 — Curie does not create the GitHub webhook, so this is an exposed API, not a confirmed push path
--    Repo binding               platform API not reached — could not discover it from the release; pass --api-url/--api-key to include this

No bundle here. Start with `curie init my-agent`.
3 things to set up. Run `curie` with no arguments for a guided walkthrough, or fix them one at a time with the commands above.
